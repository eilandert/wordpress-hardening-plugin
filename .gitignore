
# integration test runtime logs
tests/logs/

# go-ftw binary fetched by CI / local runs
/ftw
ftw.tgz

# generated per-run go-ftw corpus config (committed base: .ftw.yml)
tests/integration/.ftw.corpus.yml

# staged plugins dir (plugins/ + ci-plugin/ merged at test time)
tests/integration/.plugins-staged/
